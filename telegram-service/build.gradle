dependencies {
    implementation 'org.springframework.boot:spring-boot-starter'
    implementation "org.telegram:telegrambots-spring-boot-starter:${versions.telegram}"
}

bootJar {
    enabled = true
}